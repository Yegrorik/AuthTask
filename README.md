# FastAPI Authentication Service

Простой микросервис аутентификации и управления пользователями на FastAPI с PostgreSQL.

## 🚀 Функциональность

### Аутентификация
- **Регистрация** с проверкой паролей
- **Логин** с JWT токенами
- **Логаут** с очисткой cookies
- Хеширование паролей с Argon2

### Управление пользователями
- Просмотр и редактирование своего профиля
- Мягкое удаление (soft delete) аккаунта
- Ролевая модель: пользователь, менеджер, администратор

### Ролевая система
- **User** - базовые права: свой профиль
- **Manager** - просмотр всех пользователей, редактирование
- **Admin** - полный контроль, назначение ролей

## 📋 Эндпоинты

### 🔐 Аутентификация (`/auth`)

#### `POST /registration`
**Регистрация нового пользователя**
- Создает пользователя с ролью "user"
- Проверяет совпадение паролей
- Хеширует пароль с Argon2
- Если email уже существует но неактивен - пересоздает

**Параметры:**
- `name`, `surname` - имя и фамилия
- `email` - email (уникальный)
- `password`, `again_password` - пароль и подтверждение

#### `POST /login` 
**Вход в систему**
- Проверяет email и пароль
- Создает JWT токен
- Устанавливает токен в httpOnly cookie

**Параметры:**
- `email`, `password` - учетные данные

#### `POST /logout`
**Выход из системы**
- Очищает access_token cookie
- Требует авторизации

---

### 👤 Для всех авторизованных пользователей

#### `GET /user/profile`
**Получить свой профиль**
- Возвращает данные текущего пользователя

#### `PUT /user/profile/update` 
**Обновить свой профиль**
- Изменение имени, фамилии
- Для смены email требуется пароль

**Параметры:**
- `update_data` - данные для обновления
- `password` - пароль (требуется для смены email)

#### `DELETE /user/delete`
**Удалить свой аккаунт**
- Мягкое удаление (is_active = false)
- Автоматический логаут

---

### 👨‍💼 Для менеджеров и администраторов

#### `GET /users/get`
**Получить список всех пользователей**
- Возвращает всех активных пользователей
- Расширенные данные (включая роль)

#### `GET /user/get/{user_id}`
**Получить пользователя по ID**
- Детальная информация о конкретном пользователе

#### `PUT /user/update/{user_id}`
**Обновить данные пользователя**
- Менеджер может редактировать пользователей с равной или меньшей ролью

**Параметры:**
- `user_id` - ID пользователя
- `update_user_data` - данные для обновления

---

### 👑 Только для администраторов

#### `PUT /user/put/{user_id}`
**Назначить роль пользователю**
- Может назначать роли "user" и "manager"
- Не может назначать "admin" или понижать других админов

**Параметры:**
- `user_id` - ID пользователя  
- `role` - новая роль ("user" или "manager")

#### `DELETE /user/delete/{user_id}`
**Удалить пользователя**
- Мягкое удаление пользователя
- Не может удалить других администраторов

---

### 🔍 Проверка ролей

#### `GET /admin-check`
**Доступ только для администраторов**

#### `GET /manager-check` 
**Доступ для менеджеров и администраторов**

## 🛠 Технологии

- **FastAPI** - веб-фреймворк
- **PostgreSQL** - база данных
- **SQLAlchemy + Alembic** - ORM и миграции
- **JWT** - аутентификация
- **Argon2** - хеширование паролей
- **Docker** - контейнеризация
- **Pydantic** - валидация данных

## 🚀 Запуск

1. Клонируйте репозиторий:
    ```bash
    git clone https://github.com/Yegrorik/AuthTask.git

2. Переходим в директорию с проектом:
   ```bash
   cd AuthTask

3. Создайте файл окружения:
   ```bash
   copy .env.sample .env

4. Обновите следующие значения в `.env`:
   ```text
    DB_NAME: Name of your Data Base
    DB_USER: user data base
    DB_PASSWORD=Your PostgreSQL password
    SECRET: Your secret key for encoding tokens

5. Build с помощью Docker:
   ```bash
   docker-compose build --no-cache

6. Запуск с помощью Docker Compose:
   ```bash
   docker-compose up

7. Доступ к приложению:
    - **Документация**: localhost:8000/docs


## 🌐 Глобальный доступ

Для глобального доступа к API используется Cloudflare Tunnel. После запуска приложения публичная ссылка будет доступна в логах:
   ```bash
   docker-compose build --no-cache
   ```

---

## 👥 Тестовые данные

После запуска приложения автоматически создаются тестовые пользователи:

### 🔐 Учетные данные для тестирования:

| Роль | Email | Пароль | Описание |
|------|-------|--------|----------|
| **Admin** | `admin@example.com` | `admin123` | Полный доступ ко всем функциям |
| **Manager** | `manager@example.com` | `manager123` | Управление пользователями |
| **User** | `user@example.com` | `user123` | Базовые права |
